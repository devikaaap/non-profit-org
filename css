body {
    font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0;
    padding: 0;
    background-image: 'npo.jpg';
    background-size: cover;
    background-position: center;
}

ul {
    list-style-type: none;
    margin: 0;
    padding: 0;
}

/* Navigation Bar Styles */
nav {
    background-color: #333;
}

nav ul {
    display: flex;
}

nav li {
    margin-right: 20px;
}

nav a {
    color: white;
    text-decoration: none;
    padding: 10px;
}

nav a:hover {
    background-color: #555;
}

/* Section Styles */
.section {
    padding: 50px 0;
    text-align: center;
    font-size: larger;
    text-decoration: wavy;
}

/* Event Styles */
.event {
    border: 1px solid #ccc;
    padding: 20px;
    margin-bottom: 20px;
    border-radius: 5px;
}

.event h3 {
    margin-top: 0;
}

.event p {
    margin-bottom: 5px;
}
.image {
    display: flex;
    justify-content: center;
    
    height: 50vh; 
}
img {
    max-width: 50%; /* Ensure the image doesn't exceed container width */
    max-height: 50%;
}
